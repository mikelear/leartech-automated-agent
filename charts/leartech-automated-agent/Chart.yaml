apiVersion: v2
name: leartech-automated-agent
description: Criteria-driven agent runtime exposed as a long-running FastAPI service.
type: application
version: 0.10.140
appVersion: "0.10.140"

# leartech-helm-library helpers (_ingress.tpl) are inlined in templates/ rather
# than consumed as a chart dependency. Chart.yaml doesn't support env-var
# templating, so oci://{{ .JX_CHART_REPOSITORY }} can't resolve — helmfile
# only templates .gotmpl files. TODO: re-wire as a chart dep once
# leartech-helm-library is published via a helm-repo index (GitHub Pages or
# similar) — then `repository: https://mikelear.github.io/leartech-helm-library`
# would work without templating.
